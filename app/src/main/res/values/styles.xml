<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomDialog" parent="android:style/Theme.Dialog">

        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="Theme.UMDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="Animation.Popup" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="popwin_anim_style">
        <item name="android:windowEnterAnimation">@anim/popwin_show</item>
        <item name="android:windowExitAnimation">@anim/popwin_hide</item>
    </style>

    <style name="popwin_anim_shared">
        <item name="android:windowEnterAnimation">@anim/popwin_shared_start</item>
        <item name="android:windowExitAnimation">@anim/popwin_shared_end</item>
    </style>

    <style name="alert_dialog" parent="android:Theme.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@color/float_transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <style name="weibosdk_ContentOverlay" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <!--<span style="background-color: #00ff00;">没有标题</span>-->
        <item name="android:windowIsTranslucent">true</item>
        <!--<span style="background-color: #00ff00;">透明</span>-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--<span style="background-color: #00ff00;">窗口背景色</span>-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
        <!--<span style="background-color: #00ff00;">重要，Dialog进出的显示动画</span>-->
        <item name="android:windowContentOverlay">@null</item>
        <!--<span style="background-color: #00ff00;">是否有覆盖</span>-->
        <item name="android:backgroundDimEnabled">false</item>
        <!--<span style="background-color: #00ff00;">dialog的背景是否昏暗</span>-->
    </style>

</resources>
